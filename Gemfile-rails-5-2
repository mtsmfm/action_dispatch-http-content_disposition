source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in action_dispatch-http-content_disposition.gemspec
gemspec

gem "rails", "~> 5.2.0"
